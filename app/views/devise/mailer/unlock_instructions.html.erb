<p><%= t('devise.mailer.unlock_instructions.greeting', email: @resource.email)%></p>

<p><%= t('devise.mailer.unlock_instructions.account_locked') %></p>

<p><%= t('devise.mailer.unlock_instructions.click_link') %>:</p>

<p><%= link_to t('devise.mailer.unlock_instructions.unlock'), unlock_url(@resource, unlock_token: @resource.unlock_token) %></p>
