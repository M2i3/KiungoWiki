<p><%=  t('devise.mailer.confirmation_instructions.welcome_message', email: @resource.email %></p>

<p><%= t('devise.mailer.confirmation_instructions.confirm_message'))%>:</p>

<p><%= link_to t('devise.mailer.confirmation_instructions.confirm'), confirmation_url(@resource, confirmation_token: @resource.confirmation_token) %></p>
