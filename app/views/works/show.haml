= title(@work.title)
.row
  = render partial: 'links'
  .span12
    = render partial: 'info'
    - if @work.supplementary_sections.size > 0 || !@work.lyrics.blank?
      = link_to t('report_content'), report_work_path(@work)