<%= title(@work.title) %>

<div class="section summary">
  <p><%= Work.human_attribute_name("date_written") %>: <%= @work.date_written %></p>
  <p><%= Work.human_attribute_name("copyright") %>: <%= @work.copyright %></p>
  <p><%= Work.human_attribute_name("language_name") %>: <%= @work.language_name %></p>
  <p><%= Work.human_attribute_name("publisher") %>: <%= @work.publisher %></p>
</div>

<table>
  <thead><tr>
    <th><%= Artist.model_name.human.pluralize %></th>
  </tr></thead>
  <tbody>
    <% @work.artist_wiki_links.each do |awl| %>
    <tr>
      <td><%= artist_wiki_link_path(awl) %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<h2><%= Recording.model_name.human.pluralize %></h2>
<% unless @work.recording_wiki_links.blank? %>
<table>
  <tbody>
  	<tr>
	  	<th><%= Work.human_attribute_name("title") %></th>
  		<th><%= Recording.human_attribute_name("recording_date") %></th>
  		<th><%= Recording.human_attribute_name("recording_location") %></th>
  		<th><%= Recording.human_attribute_name("duration") %></th>
  		<th><%= Recording.human_attribute_name("rythm") %></th>
  		<th><%= Recording.human_attribute_name("sample_rate") %></th>
  	</tr>
    <% @work.recording_wiki_links.each do |recording_wiki_link|
    	begin
		  	recording = Recording.find(recording_wiki_link.searchref[:oid]) %>
				<tr>
					<td><%= recording_wiki_link_path(recording_wiki_link) %></td>
					<td><%= recording.recording_date %></td>
					<td><%= recording.recording_location %></td>
					<td><%= recording.duration %></td>
					<td><%= recording.rythm %></td>
					<td><%= recording.sample_rate %></td>
				</tr>
			<% rescue %>
					<tr>
						<td><%= recording_wiki_link_path(recording_wiki_link) %></td>
						<td></td>
						<td></td>
						<td></td>
						<td></td>
						<td></td>
					</tr>
			<% end %>					
    <% end %>
  </tbody>
</table>
<% else %>
	<!-- TODO: What do we want to show when a work has no recordings? -->
<% end %>

<h2><%= Work.human_attribute_name("lyrics") %></h2>
<p><%= content_tag('pre', @work.lyrics )%></p>
<h2><%= Work.human_attribute_name("chords") %></h2>
<p><%= @work.chords %></p>

<%= render :partial=>'links' %>


