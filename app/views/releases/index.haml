.page-header
  = title(Release.model_name.human.pluralize)

- unless params[:q]
  .row
    .span12
      = alpha_release_links
- else			
  .row
    .span12
      = t("search_question")
      = link_to t("new"), new_release_path(q: params[:q]), class: "btn"

= paginate @releases
= render partial: "search_result", object: @releases
= paginate @releases

- unless params[:q]
  .row
    .span12
      = alpha_release_links

