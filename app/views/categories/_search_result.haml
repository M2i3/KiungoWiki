%table.table.table-striped
  %thead
    %tr
      %th= Category.human_attribute_name("category")
    %tbody
      - search_result.each do |category|
        %tr
          %td
            = link_to category.category_name, category_path(category)