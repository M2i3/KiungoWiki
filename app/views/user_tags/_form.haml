= form_for([@taggable, @user_tag]) do |f|
  - if @user_tag.errors.any?
    #error_explanation
      %h2
        = t('messages.error')
      %ul
        - @user_tag.errors.full_messages.each do |msg|
          %li
            = msg
  %p
    = f.label :taggable, UserTag.human_attribute_name("taggable")
    = f.text_field :taggable, 'data-pre' => @user_tag.taggable? ? [{id:@user_tag.taggable, name:@user_tag.taggable.title}].to_json : "", class: "release_title_lookup"
  %p
    = f.label :name, UserTag.human_attribute_name("name")
    = f.text_field :name
  .actions
    = f.submit submit_text
